version: '3.8'
services:
  frontend:
    build: ./client
    ports:
      - "3000:3000"
    networks:
      - webapp

  # backend:
  #   build: ./server
  #   hostname: api
  #   networks:
  #     - webapp
  #   ports:
  #     - "8080:8080"

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - '27017:27017' # Remove during prod


networks:
  webapp:
    driver: bridge